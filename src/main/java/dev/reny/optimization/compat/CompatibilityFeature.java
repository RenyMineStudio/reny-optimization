package dev.reny.optimization.compat;

/** Functional areas that may overlap with another optimization/rendering mod. */
public enum CompatibilityFeature {
    RENDERER,
    CHUNK_RENDERER,
    SHADER_PIPELINE,
    LIGHTING_ENGINE,
    TEXTURE_PIPELINE,
    OPENGL_BACKEND
}
